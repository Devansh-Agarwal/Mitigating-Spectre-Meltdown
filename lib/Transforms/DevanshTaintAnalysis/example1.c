// Type your code here, or load an example.
#include <stdio.h>

void number(int *a , int l , int d)
{
	int k ;
	k =   *a + 2;
	l = l + l;
	k += d;
	k += l;
	l += 2;
  if( *a  > 2132)
    k++;
	int zzz = k;
	zzz += 2;
} 
int main()
{

    int  a,b,c, d, e = 22, ssss = 121, untaint = 222, untaint2 = 2321;
    scanf("%d, %d",&a, &b);
    scanf("%d",&a);
    scanf("%d, %d",&c, &d);
    if( a > b)
    	 c = 5  + 2;
    e = d+ 22;	
    ssss = 321321;
	number(&a , e, d);   
    if(untaint < 2112)
    {
      untaint *= 21;
    }
    if(untaint < 2112)
    {
      d *= 21;
      ssss *= 21;
    }
  	if(ssss > 222)
  	{
  		ssss++;
  	}
  	if(ssss > d)
  	{
  		ssss++;
  	}
    return 0;
}